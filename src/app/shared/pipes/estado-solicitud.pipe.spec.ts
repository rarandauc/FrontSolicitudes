import { EstadoSolicitudPipe } from './estado-solicitud.pipe';

describe('EstadoSolicitudPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoSolicitudPipe();
    expect(pipe).toBeTruthy();
  });
});
