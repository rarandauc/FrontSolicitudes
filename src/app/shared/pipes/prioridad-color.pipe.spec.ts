import { PrioridadColorPipe } from './prioridad-color.pipe';

describe('PrioridadColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PrioridadColorPipe();
    expect(pipe).toBeTruthy();
  });
});
